// Primatives
// Booleans: either T or F
const isAdult = true;
const canDrink = false;

// Null: empty value
const nothing = null;

// Undefined: no value assigned
let name = "";
name = "Jimmy";

// Reference Error: 'not defined' (not the same as 'undefined')
console.log(whatever);
