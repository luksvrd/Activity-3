// This is a comparison oporator - always evaluate to a boolean value T or F.
console.log(3 < 2); // false

const total = 30;
console.log(total <= 3); // false
console.log(3 <= 3); // true

console.log(total >= 30); // true
// === is a comparison equal (like asking are theses values T or F?)
console.log(total === otherTotal); // false
